{
  "name": "test",
  "version": "1.0.0",
  "description": "",
  "main": "test.bundle.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "rm -rf .parcel-cache dist && parcel test.html"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "platinum": "file:.."
  },
  "devDependencies": {
    "parcel": "^2.5.0"
  }
}
